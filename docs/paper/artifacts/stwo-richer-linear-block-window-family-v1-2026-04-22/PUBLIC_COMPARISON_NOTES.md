# Public Comparison Notes

This bundle should be read next to `docs/paper/phase114-public-benchmark-context-table-2026-04-22.md`, which separates:

- exact internal frozen artifact metrics, and
- public-paper architecture-context rows.

Narrow claim only: after freezing the repeated-window Phase107 source and the Phase112 semantic handoff, the same ordered leaf family can be reduced to a compact richer-family artifact that restores normalization and activation family commitments.

Safe public positioning:

- `zkLLM` matters because it specializes non-arithmetic tensor operations and attention and reports a full 13B-parameter inference proof with sub-200 kB proofs in under 15 minutes.
- `Jolt Atlas` matters because it moves directly to ONNX tensor relations and emphasizes streaming, memory-constrained proving rather than VM emulation.
- `NANOZK` matters because it treats layerwise transformer proving as a serious public route and reports 5.5 KB layer proofs with 24 ms verification at `d = 128`.
- `ZKTorch` matters because it combines specialized basic blocks with parallel accumulation and reports proof-size and proving-time gains against other zkML baselines.

This bundle should not be described as recursive compression, generic STARK folding, a matched public benchmark win, or a like-for-like proof-size comparison against those systems.
