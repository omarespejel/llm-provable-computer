# Public Comparison Notes

Narrow claim only: after freezing the repeated-window fold tree, the same ordered Phase107 leaf family can be reduced to a much smaller transformer-specific accumulation-semantics handoff.

This should not be described as recursive compression, generic STARK folding, or production-scale transformer proving. It is a smaller verifier-bound semantic surface for later accumulation layers.
