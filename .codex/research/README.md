# Research OS

Agent entrypoint for the STARK-native zkML research program.

Use the structured files as source of truth:

1. `north_star.yml`
2. `operating_model.yml`
3. `frontier_ledger.jsonl` when present
4. `agent_briefs/*.yml`
5. `schemas/*.schema.json`

Markdown notes are secondary. If prose conflicts with structured files, follow
the structured files and open a follow-up issue for the drift.
